apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prometheus-storage-pvc
spec:
  accessModes:
    - ReadWriteOnce
  # Minikube automatically provides standard hostpath storage
  storageClassName: standard 
  resources:
    requests:
      storage: 2Gi # Allocate 2 Gigabytes for metrics database

